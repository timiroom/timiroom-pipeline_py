[pytest]
# 프로젝트 루트를 sys.path에 넣는다.
# `python -m pytest`는 cwd를 자동으로 넣어주지만 `pytest`(CI가 실행하는 형태)는 넣지 않아
# common/config/routers 임포트가 ModuleNotFoundError로 깨진다.
pythonpath = .

# 수집 범위를 tests/ 로 한정한다. 루트의 test_solar.py 는 pytest 대상이 아니라
# 손으로 돌리는 임베딩 점검 스크립트인데 파일명이 test_* 라 딸려 들어온다.
testpaths = tests
